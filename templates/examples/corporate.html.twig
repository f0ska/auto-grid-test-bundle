{% extends '@F0skaAutoGridTest/base.html.twig' %}
{% import '@F0skaAutoGridTest/components/theme_switcher.html.twig' as agt %}

{% block title %}Clean UI Dashboard - AutoGrid{% endblock %}

{% block content %}
    {{ agt.infoBlock('Demonstrated features:', [
        'Refining UI with custom <strong>Labels</strong> and field <strong>Positions</strong>',
        'Applying compact table styles via <code>#[HtmlClass]</code>',
        'Automatic currency formatting using <code>#[ValuePrefix]</code>',
        'Setting grid display names with <code>#[Title]</code>',
        'Global search with <code>#[Searchable]</code> across company name and contact email',
        'Single visible filter using <code>#[Filterable(additionalFields: ...)]</code> across company name and contact email',
        'Displaying non-mapped computed data via <code>#[VirtualColumn]</code> and <code>#[ViewService]</code>'
    ]) }}

    {{ ag_render(grid) }}
{% endblock %}
