{% extends '@F0skaAutoGridTest/base.html.twig' %}
{% import '@F0skaAutoGridTest/components/theme_switcher.html.twig' as agt %}

{% block title %}Custom Actions & Export - AutoGrid{% endblock %}

{% block content %}
    {% set config = agt_theme_config() %}
    {{ agt.infoBlock('Demonstrated features:', [
        'Overriding default action URLs with <strong>Custom Routes</strong>',
        'Bulk operations using <strong>Mass Actions</strong>',
        'Data export functionality (e.g. CSV generation)',
        'Inline advanced filters using <code>#[AdvancedFilter(display: \'inline\')]</code>',
        'Event-driven logic processing via <code>EventSubscriber</code>'
    ]) }}

    {{ ag_render(grid) }}
{% endblock %}
